---
topic: Two difficulty curves that are actually two curves, a tutorial that does not cost the round, and copy that stops contradicting the game
---

- event: create. Source is the player's request of 2026-09-19 plus a code audit of index.html, distilled at scratchpad/two-curves-intent.md. Mode: express - the player asked for the work, not an interrogation.
- direction: Three jobs, in the player's order. Fix the four copy sites that state things the game stopped doing. Fix the tutorial properly. Make SCHOLAR the easy mode and APPRENTICE hard-but-not-too-hard.
- decision: Take the player's name assignment literally. SCHOLAR = easy, APPRENTICE = hard. It runs against the ordinary sense of the words, so each mode's own description must state what it is outright and nothing may depend on reading the name correctly.
- assumption: The player meant the roles to swap rather than "retune the two modes you already have under their current names". Recorded because it is the one reading that changes user-visible labels; the tuning work underneath is identical either way and the label is a one-line data change if this is wrong.
- note: Measured baseline before any change, tools/balance.js 300 --play optimal --buy value. SCHOLAR 19.0% win (95% CI 14.6-23.4). APPRENTICE 51.3% win (95% CI 45.6-57.0).
- insight: The current APPRENTICE is already close to the easy mode the player is asking for - 51.3% against a ~55% goal, and its death curve climbs monotonically (0.3/1.0/5.0/6.3/10.7% by round). Its shape is right. It mostly needs renaming and a small lift, not redesigning.
- insight: The current SCHOLAR is not hard so much as SPIKED. Round 2 ends 32.0% of all runs - nearly a third, after exactly one shop visit - then rounds 3/4/5 end 13/15/7%. That is a wall in the wrong place, not a difficulty curve. Targets climb a flat 1.30x a round while deck power does not: after round 1 a player owns one Lens.
- decision: The hard mode's job is to move that danger, not to add more of it. Flatten the early ramp and let the back half carry the run's risk, since Lens growth compounds and the endgame is where a built deck should be tested.
- decision: Difficulty becomes DATA. Each DIFFICULTIES entry carries its own curve, discards, ordeal schedule, ordeal ban, reckoning flag, unlock rule, storage key and copy; every call site reads the property instead of testing the id.
- constraint: 24 sites in index.html currently branch on `id === "apprentice"` or `id === "scholar"`. Swapping roles by editing two numbers would leave every one of them meaning the opposite of what it says. The refactor is a precondition for the retune, not a nicety.
- note: APPRENTICE_CURVE holds 8 entries for a 6-round run. Two are dead, left over from the eight-round era.
- decision: Existing players migrate by ROLE, not by name. Somebody who chose the gentle curve keeps the gentle curve and sees its new name; somebody who chose the steep curve keeps the steep curve. Best scores move with the role for the same reason - a record set on a curve is a record on that curve.
- capability: CAP-1 - the board stops contradicting itself. Four copy sites state things the game no longer does.
- capability: CAP-2 - the tutorial does not cost the player the round it teaches in.
- capability: CAP-3 - difficulty is data, not branches.
- capability: CAP-4 - an easy mode that is actually easy.
- capability: CAP-5 - a hard mode that is losable rather than walled.
- capability: CAP-6 - existing players are migrated deliberately rather than silently.
- constraint: Every win-rate number is MEASURED with tools/balance.js against the real engine, never asserted. n and the 95% CI are stated with the figure (SE = sqrt(p(1-p)/n)).
- decision: This spec supersedes the SCHOLAR figure in spec-the-verb-is-join CAP-7 ("SCHOLAR win rate inside [15%, 30%]"). That bound now belongs to the HARD mode whatever it is called. The sibling spec is adopted as a companion and is not edited.
- constraint: Nothing in the JOIN engine moves. joinOf, joinsOf, figuresFromJoins, resolve, the five hands and their payouts, fatigue and kept lines are all out of scope.
- insight: Round 2 on the steep curve is a COMPOUND wall, not one number. ORDEAL_ROUNDS = [1,3,5] puts an Ordeal on round 2, it is the largest relative target step a player has yet met, and only one shop visit sits behind it. Moving the first Ordeal to round 3 is half the fix; flattening the opening step is the other half.
- decision: Steep mode's ordealRounds move from [1,3,5] to [2,3,5]. Gentle keeps [5].
- decision: Gentle curve starting point [0.52,0.52,0.54,0.56,0.59,0.61]; steep [0.95,0.82,0.86,0.90,0.93,0.95]. Both are starting points to be measured, not the answer.
- note: Today's gentle curve dips - its round 2 multiplier of 0.31 makes round 2 easier in absolute points than round 1 (3,111 against 4,632). The replacement climbs monotonically.
- event: self-validate pass 1 (coherence, Spec Law 1-6 and 8). PASS. Six capabilities each carry intent and success; four constraints each rule something out; five non-goals; the success signal is numeric and testable.
- event: self-validate pass 2 (preservation). PASS with one note. Every load-bearing source claim landed: four copy sites in CAP-1, the tutorial's play cost in CAP-2, the 24 id-branches and the stale 8-entry curve in the companion, the two curves in CAP-4/CAP-5, migration in CAP-6, the naming concern in assumptions. NOTE: the tutorial's stale step-4 wording and its hand rearrangement are covered only by CAP-1's general "every instruction describes what the game does" and are not in CAP-1's grep-shaped success criteria; they are checked by walking the tutorial, not by grep.
- note: MEASURED gentle curve [0.52,0.52,0.54,0.56,0.59,0.61] at n=500 optimal/value: 66.0% win (95% CI 61.8-70.2). Above CAP-4's [48,62] band - too soft. Death curve fine: 0.2/1.8/5.0/4.8/4.8, all under the 15% cap.
- decision: Gentle curve raised at the back to [0.54,0.54,0.58,0.62,0.68,0.75] -> absolute [4169,5419,7567,10516,14993,21498]. The old gentle curve reached only 51.3% with a 0.86 round-6 multiplier, so the back half is what carries the win rate.
- note: MEASURED steep curve [0.95,0.82,0.86,0.90,0.93,0.95] with ordealRounds [2,3,5] at n=500: 18.0% win (95% CI 14.6-21.4). Below CAP-5's [25,38] band.
- insight: The round-2 spike MOVED rather than vanished - round 2 fell from 32.0% to 7.2%, but round 3 rose to 31.8% and round 4 to 21.0%. The Ordeal is the wall, not the target, and [2,3,5] put two Ordeals back to back at rounds 3 and 4. Confirms the compound-wall reading: an Ordeal landing on a big target step is what ends runs.
- decision: Took the companion's documented fallback. Steep drops to two Ordeals at [2,5] - rounds 3 and 6, never adjacent - and the curve eases to [0.92,0.80,0.82,0.85,0.89,0.93] -> absolute [7102,8029,10699,14417,19624,26658].
- note: MEASURED steep [0.92,0.80,0.82,0.85,0.89,0.93] with ordealRounds [2,5] at n=500: 24.8% win (95% CI 21.0-28.6). Just under CAP-5's band, and round 3 still ends 30.4% of runs.
- note: MEASURED gentle [0.54,0.54,0.58,0.62,0.68,0.75] at n=500: 53.0% win (95% CI 48.6-57.4). INSIDE CAP-4's [48,62] band. Worst pre-final round 7.4%. CAP-4 passes.
- decision: CORRECTION to this spec's own acceptance. The "no single round ends more than X%" cap cannot bind the FINAL round: balance.js reports the round a run ended on including wins, so every surviving run resolves at the last round by construction and at a 53% win rate it necessarily ends ~27% of runs. A cap there would be a cap on the win rate. Restated as "no round BEFORE THE LAST". Under the corrected reading gentle passes (max pre-final 7.4%) and steep still fails on round 3 (30.4%), so the correction does not excuse the real defect.
- insight: Moving the first Ordeal from round 2 to round 3 moved the wall with it, again - round 3 went from ~13% to 30.4%. An Ordeal in the early-middle lands on a deck that has not been built yet. The wall is not any particular round; it is "first Ordeal, few shops behind it".
- decision: First Ordeal moves to round 4 (ordealRounds [3,5]) where three shops sit behind it, and the steep curve eases to [0.90,0.78,0.80,0.82,0.86,0.90].
- note: MEASURED steep [0.90,0.78,0.80,0.82,0.86,0.90] with ordealRounds [3,5] at n=500: 26.0% win (95% CI 22.2-29.8) - inside CAP-5's band - but round 4 ends 26.0%, over the 20% cap.
- insight: THE WALL FOLLOWS THE FIRST ORDEAL, wherever it is put. Round 2 ended 32.0%, moved to round 3 it ended 30.4%, moved to round 4 it ended 26.0%. Three measurements, one cause. The round index was never the variable; "a rule change landing on a big target step" is.
- insight: The file already carried the fix, as a comment describing behaviour that had stopped happening - "Rounds 4, 6 and 8 are the Ordeals, and their numbers climb least... an Ordeal takes a tool away rather than asking for more, so the round gets its difficulty from its rule." That intent was lost when TARGETS became a flat 1.30 ramp and nothing re-derived the per-round shape.
- decision: The steep curve flattens AT its Ordeal rounds rather than moving them. [0.90,0.80,0.85,0.78,0.83,0.77] steps +38% on ordinary rounds and +19/+21% on the two Ordeal rounds, so an Ordeal round's difficulty is its rule and not its number. Every steep target still sits above the gentle one at the same round.
- assumption: The two curves' raw targets are allowed to sit close. Most of the steep mode's extra difficulty comes from its RULES - three discards instead of four, two Ordeals instead of one with none banned, and THE RECKONING - rather than from its numbers. Gentle needs a high endpoint to score only 53%, so the numbers converge; that is the design working, not a bug.
- note: MEASURED steep [0.90,0.80,0.85,0.78,0.83,0.77] (flattened at Ordeal rounds) at n=500: 31.2% win (95% CI 27.1-35.3) - INSIDE CAP-5's band. But round 4 still ended 24.4%, over the 20% cap: flattening the target moved the win rate 26.0 -> 31.2 and barely moved the wall.
- insight: The target was never what made an Ordeal round lethal. An Ordeal that DISARMS - takes discards, cards, plays, or the worth of a word on its own - cannot be answered by a player two shops in, whatever the number says.
- decision: Added a per-SLOT Ordeal pool. The last scheduled Ordeal of a run may be anything; every earlier one squeezes rather than disarms. This is the same distinction the gentler mode's ordealBan already drew, applied to the slot instead of the mode.
- event: DEFECT FOUND. THE CLOCK read `g.plays = 3` with the description "Three plays this round instead of four". A round has been two plays since the JOIN rebuild, so the Ordeal HANDED THE PLAYER A PLAY while describing a number the game no longer used. Rewritten to take one, floored at one, described as "One play fewer this round".
- decision: THE CLOCK counts as disarming. Against four plays "one fewer" was a squeeze; against two it means clearing the whole round in a single hand.
- event: DEFECT FOUND. The help card quoted TARGETS[0] and TARGETS[ROUNDS-1] raw - the steepest curve's numbers read out to a player standing on a gentler one. It told a SCHOLAR player round 1 wants 7,720 when it wants 4,169. Now scaled by the mode's own curve.
- note: Ordeals generally were tuned for a four-play, three-word round and have drifted. THE VICE caps a line at 2 of a possible 5 rather than 2 of 3; THE TOLL's 90 points a word is now noise against four-figure targets. Only THE CLOCK was outright broken and only it was fixed; a full Ordeal re-derivation is follow-on work and is named as such.
- note: MEASURED steep with per-slot Ordeal pools at n=500: 26.8% win (95% CI 22.9-30.7), INSIDE CAP-5's band. Pre-final rounds end 3.2/6.0/16.0/18.6/8.6% - every one under the 20% cap, round 4 down from 24.4%. Deaths in all six rounds. CAP-5 PASSES.
- insight: The per-slot pool was what finally moved the wall, not the target. Round 4 went 26.0 -> 24.4 on a flattened target and 24.4 -> 18.6 once its Ordeal could no longer be a disarming one.
- note: Gentle fell 53.0% -> 48.4% after THE CLOCK was fixed, because its single round-6 Ordeal can be THE CLOCK and that Ordeal used to HAND IT A PLAY. Back end eased to [0.54,0.54,0.58,0.62,0.66,0.70] and re-measured.
- event: CAP-2 verified in the browser and by spec - a tutorial-completing run ends the lesson with 2 plays, the same as a skipping run, and keeps the 1,640 the taught hand scored. 3/3 e2e.
- event: CAP-3 verified - zero difficulty-id string literals outside DIFFICULTIES and the migration block. CAP-6 verified - 5/5 migration e2e, including idempotency across a reload.
- decision: OPEN QUESTION RESOLVED (the unlock gate). The gentler mode is always open (lockedUntilRun 0) and the steeper one opens after a finished run (lockedUntilRun 1). Same rule as before the swap - the steep curve is the one a first screen should not offer - but expressed as a property instead of a function named after one mode.
- event: DEFECT FOUND. Three board strings still pointed at the panel by its old name after it was renamed - "see the figures" on the stage, "All five figures" in the help card, and "The figures" on the title shelf. All now read "the five hands".
- note: FINAL MEASURED gentle [0.54,0.54,0.58,0.62,0.66,0.70] at n=500: 53.4% win (95% CI 49.0-57.8). Pre-final rounds end 0.4/1.6/4.4/6.0/6.4%, all under the 15% cap. Deaths in all six rounds. CAP-4 PASSES.
- note: FINAL MEASURED steep [0.90,0.80,0.85,0.78,0.83,0.77], ordealRounds [3,5], per-slot pools, at n=500: 26.8% win (95% CI 22.9-30.7). Pre-final rounds end 3.2/6.0/16.0/18.6/8.6%, all under the 20% cap. CAP-5 PASSES.
- decision: Scoped the tutorial refund to its own run - tutSpentPlay is cleared in newRun() and startTutorial(). Without it a player who started a new run mid-lesson carried the debt across and was handed a play the new round never spent.
- event: spec finalised. All six capabilities met and measured. Follow-on named but not done: the Ordeals as a set were derived against a four-play, three-word round and only THE CLOCK was outright broken enough to fix here.
- event: bmad-review run (adversarial + edge-case lenses). 15 + 26 findings. The serious ones were real.
- event: DEFECT FOUND AND FIXED (review, confirmed exploit). The tutorial refund was unlimited. Help always offers "Run the tutorial", so a player could open it in any round, play a hand, press Skip and be handed the play back, repeatedly. Every measured win rate described a game a player could opt out of. Refund is now gated: the opening lesson only (round 0, nothing scored), once per run (G.tutRefunded), never on a second PLAY inside the lesson, and never on a round the taught hand already cleared.
- event: DEFECT FOUND AND FIXED (review). tests/harness.js seeded a difficulty but not curvesMigrated, so the one-time role swap fired on every load() and handed back the opposite mode. Any balance.js run without --difficulty measured the other curve.
- event: DEFECT FOUND AND FIXED (review). A profile with runs>0 and no stored difficulty was silently moved from the steep curve to the gentle one - the exact CAP-6 violation the migration exists to prevent. It now writes the steep id for that case.
- event: DEFECT FOUND AND FIXED (review). The Bookseller's "x the usual" warning compared against raw TARGETS, and no curve entry is 1.0, so it fired on EVERY shop visit in gold, blaming the mode's own curve on the Lenses held. Compares against the mode's own curve now. Its shape line had the same bug against a hardcoded 3 discards.
- decision: PLAYS_PER_ROUND and DISCARDS_PER_ROUND are constants. The round's budget was a literal in three places, which is how a comparison against "3 discards" survived a mode that starts every round with four.
- decision: REVIEW FINDING REJECTED, with measurement. The adversarial lens read ordealBan and DISARMING as the same distinction and asked that the gentler mode ban both. They do different jobs: ordealBan is about what a rule DELETES (a mechanic the tutorial taught), DISARMING is about WHEN it lands (an early slot a two-shop deck cannot answer). Setting ordealBan to DISARMING measured 65.6% - out of band, and a last round with nothing in it. Reverted and both concepts documented in place.
- event: curveScale now throws rather than falling back, and shapeFor returns null past the end of the run rather than a NaN target that reached the Bookseller as the string "NaN". reckonFor guards on it.
- event: Node suite restored to its pre-change baseline - 136 pass / 33 fail, against 33 failing before this work and 44 during it. apprentice, reckoning, flaws and runend were rewritten to read mode properties; figures/lenses/logic failures are the JOIN-rebuild backlog the spec scopes out.
- event: public/ (the Firebase deploy build, gitignored) still held the pre-change game including ORDEAL_ROUNDS [3,5,7] and scholarOpen(). Regenerated. Not deployed - nobody asked for that.
- note: FINAL CONFIRMED after every review fix, n=500: gentle 53.4% (95% CI 49.0-57.8), steep 26.8% (95% CI 22.9-30.7). Both in band, every pre-final round under its cap.
- event: DEFECT FOUND AND FIXED. tests/e2e/fullrun.spec.js tapped cards 1, 2 and 3 and pressed Enter. Under PICK that approximated a player; under JOIN the order IS the decision and a silence stops the line, so three cards in dealt order could not clear round 1 and the walk ended before it ever reached a shop - which is what its own assertions are about. It picks with bestLineFrom() now, and its play-count floor moved from the eight-rounds-of-four it was written against.
- note: E2E after all fixes: 159 passed / 22 failed / 7 skipped, against a 148-passed baseline. Eleven tests added (tutorial refund, the replay exploit, migration, the name pin) and eight previously-failing ones fixed. The remaining 22 are assertions written for the pre-JOIN game - four plays a round, the spelling figures, the old help copy - which SPEC scopes out as separate work.
- note: Two of them were checked against HEAD byte for byte to confirm they are not this change's doing: "Take three words" and playsLeft === "4" are identical in the committed file.
- direction: Player asked "wasnt scholar supposed to be hard and apprentice easy" - the assumption recorded at the top of this log, and flagged in every report since, is wrong. APPRENTICE is the gentle curve and SCHOLAR the steep one.
- decision: SUPERSEDES the literal-names decision. The two ids exchange property sets; each keeps its own display name. This RESTORES the polarity the game shipped with before this spec, so it is a revert of the flip, not a new flip.
- decision: The storage migration loses its difficulty-id swap entirely. The flip never reached a player - nothing was committed and playovertone.web.app was never deployed - so there is nobody on a swapped id to move back. What survives is the best-score key copy (best:apprentice -> best:gentle, best -> best:steep), which is keyed to CURVE and was never name-dependent, plus the runs>0-with-no-stored-difficulty branch.
- note: The name-pinning e2e test existed only because the migration moved people between two named modes. Replaced with its inverse: the migration must NOT move anybody off the mode they chose.
- note: CAP-3's refactor is what made this a data edit. Every property travelled with its id untouched - APPRENTICE re-measured at 53.4% with a histogram identical to the pre-swap gentle figure (2/8/22/30/32/406 by round).
- event: Node suite 137 pass / 32 fail, one BETTER than the 136/33 baseline. logic.test.js "the gentler curve is gentler on every round" was written for the original polarity, had been failing silently against the flip, and the revert repairs it - evidence the original polarity is the one the codebase was built around.
- direction: Player ran a BMad gameplay review (adversarial + edge-case lenses) and then said "fix everything you have to".
- event: REVIEW FINDING REFUTED BEFORE ACTING ON IT. The adversarial lens claimed a mechanical tag-chaining greedy reaches 98.1% of optimal - i.e. the JOIN rebuild failed and the game is still tag matching. Implemented the greedy and measured 67.2% median, within 5% on 9% of hands (n=80). The claim was wrong and the conclusion drawn from it was withdrawn.
- note: CONFIRMED by re-measurement: ordering beats selection roughly 10 to 1. The 2nd-best five-card subset played perfectly scores 96.5% of the best; the best subset in a typical order scores 9.1% (n=25, exhaustive over 21 subsets x 120 orderings).
- event: DEFECT - the measuring instrument itself. tools/balance.js READ G.kept when scoring a line and never WROTE it, so every win rate in this spec was measured against a player who never once kept a line. Given a keep policy. Every figure below is post-fix and is the first honest measurement of either curve.
- decision: Kept lines get their own shelf, KEEP_SLOTS = 2, separate from LENS_SLOTS. They were free, unbounded and competed with Lenses for the same five slots, so "keep five, buy nothing" strictly beat every purchase and the purse stopped meaning anything.
- decision: A kept line binds to the figure that PAID most, not res.figs[0]. figs[0] is FIGURES table order and chiasmus is first, so 74 of 80 optimal lines bound to chiasmus and every kept line was the same card wearing different words.
- decision: SPELLING IS OUT OF SCORING, per spec-the-verb-is-join's first constraint. makeCard set base = 5 + 3 x LETTERS, so spelling was not leaking through four Lenses - it was the base currency of every card. Re-keyed to 6 + 5 x overtones, scaled so the lexicon's mean base is unchanged at 22. SESQUIPEDALIAN, BRUTALIST, ENTROPY and LEXICOGRAPHER re-keyed onto tag data. The card-face letter-count badge removed: it existed for COLUMN, STAIR and PAIR, which the JOIN rebuild retired.
- decision: THE VICE maxPlay 2 -> 3. Forced onto the schedule it measured 22.0% against a 73.0% ceiling (gentle) and 15.3% against 44.0% (steep) - three times the bite of the next worst rule.
- decision: The gentle mode's ordealBan is drought/lean/VICE, not drought/lean/fog. It was banning the harmless rule and permitting the lethal one, and because banning shrinks the pool it raised THE VICE's odds from 1-in-9 to 1-in-6.
- decision: THE RECKONING's drafted rule obeys the same slot guard as a dealt one. Measured before: 280 of 495 drafted rules were disarming, landing on rounds 3 and 4. After: 0 of 367.
- note: RECKON_AT left at 3 and its comment corrected instead. It claimed "0-5% of rounds"; measured 39-57% of rounds 3-6. The curves are measured with it firing at that rate, and the danger was never the frequency - it was the bypassed guard.
- decision: THE LEAN YEAR and THE CLOCK take a discard when the hand or the play count is already at its floor. THE FAMINE and THE WAGER respectively made them complete no-ops while the board still posted them in the warning colour.
- decision: ENTROPY marked flaw:true (measured 0.658x the best line without it, sold as pure upside). GLUTTON x2.5 -> x3.2: measured 1.000x, the search simply ignored it, so it was $6 and a discard for nothing.
- note: Board-honesty fixes: the counters no longer price an uncommitted line (spec constraint, untested until now); the match count applies dullTags() and the jr.scored cutoff; running chips clamp at 0 in the display; the play-pip row reads the round's budget instead of a hardcoded 4; G.best is keyed on score, not multiplier.
- note: Shop fixes: one slot-accounting helper instead of five inconsistent call sites; the dead-end copy only suggests moves the player can make; the last shop stops comparing round 6 with itself; the keep card renders disabled with a reason instead of dead-clicking.
- note: The death screen names what ended the run - the round's target, what was made, the shortfall, and the last hand played. It previously printed the Demand's name and three stats, none of which was the number that killed you.
- event: RE-TUNED. Every fix above made both curves easier, so they were re-measured and raised 1.35x. APPRENTICE [0.73,0.73,0.78,0.84,0.89,0.95], SCHOLAR [1.22,1.08,1.15,1.05,1.12,1.04].
- note: FINAL, n=500, --play optimal --buy value, against a simulator that now keeps lines: APPRENTICE 56.4% (band 48-62, worst pre-final round 8.8% against a 15% cap), SCHOLAR 27.2% (band 25-38, worst pre-final round 19.8% against a 20% cap). Both in band.
- note: Tests: Node 138 pass / 32 fail of 170 (baseline 137/32 of 169 - one added, none regressed). E2E desktop 161 passed / 21 failed / 7 skipped (baseline 160/21/7). The 21 and the 32 are the pre-JOIN backlog this spec scopes out.
- decision: play() and the PLAY button enforce minPlay() - LINE_MIN for everybody, 1 while ASCETIC or THE ORACLE is held. It lived in two copy strings and nowhere else, so the tutorial said "a line is two to five words" while the engine took one, and ASCETIC's "play exactly 1 word" was selling a play every player already had.
- direction: Player asked for a thorough check and for the four deferred review items to be fixed as well.
- decision: THE DEMAND pays multiplier as well as points - DEMAND_CHIPS 55, DEMAND_MULT 0.5 per matching overtone, up from 25 flat. Measured share of a line's chips: 5.4% -> 19.0%. Choosing the words is now an axis with weight, against ordering's 10x.
- note: DEMAND_MULT is 0.5 rather than 0.4 on purpose. Halves are exact in binary; 0.4 is not, and a hand that should read x2 was resolving to 1.9999999999999998.
- decision: THE BUILD and THE MIRROR (chiasmus) now match their own card text. escalation fired if ANY adjacent pair rose - 98% of optimal plays - and reads "each pair with more in common than the pair before it", so it needs the whole line to climb. chiasmus fired on any A-B-A window and reads "four words whose pairs read the same backwards as forwards", so the joins must mirror end to end in kind AND in shared count.
- note: Figures measured after: 0.71 per arbitrary five words, 2.12 per line the search picks (was 3.81 and effectively universal). A figure is something a player makes again, and spec-the-verb-is-join CAP-3 stops passing vacuously.
- note: Line length became a decision on its own once figures were rare and the Demand mattered - no length cost was needed. The optimum uses the full cap on 46% of hands, down from 100%; 4 and 5 words are close to a coin flip.
- decision: A round's reward adds up to $6 for beating the target, on top of 4 + what is left. It was 7 or 8 every round of every run, so no shop was ever a question of whether or when.
- event: DEFECT FOUND AND FIXED, introduced by raising the Demand. THE MIRROR cancelled "c.base + 25*hits" - the Demand's old rate - so a one-match word under a rule whose whole text is "a word scores nothing" still scored +30 chips and +0.5 mult. THE ORACLE had the same hole. Both now read one shared demandHits() and cancel the multiplier as well as the points; the Ordeal card hook gained mult support to allow it.
- note: RE-TUNED AGAIN. SCHOLAR was reshaped rather than rescaled: [0.95,0.94,1.02,0.97,1.08,1.02]. At [1.22,...] round 1 asked 9,418 of a bare deck and the final/first ratio was 3.2 - a wall at the front with little arc behind it. It is 4.0 now and the wall sits at round 3.
- note: FINAL, n=500: APPRENTICE 55.8% (band 48-62, worst pre-final round 7.2% against a 15% cap), SCHOLAR 28.8% (band 25-38, worst pre-final round 16.8% against a 20% cap).
- event: Full re-verification sweep, 18 checks, all passing: the deferred four, the VICE ban, the spelling constraint, the two cancelling rules, the line minimum, the kept-line shelf, and both curves climbing. THE RECKONING: 488 levies drafted, 403 before the last round, 0 disarming.
- note: Tests: Node 138 pass / 32 fail of 170. E2E desktop 161 passed / 21 failed / 7 skipped. Both exactly the pre-existing backlog.
- direction: Player playing the deployed build asked "WHY does the points not get counted? on the left its not showing".
- event: The counters were correct and unreadable. An uncommitted line has no score - spec-the-verb-is-join forbids showing one - and setTally(0,1) said so by printing 0 and 1, which is indistinguishable from a counter that has stopped. Blank by design has to look deliberate.
- decision: setTallyPending() prints en-dashes, dims both counters and shows "COUNTED WHEN YOU HIT PLAY" under them. The constraint is unchanged; only the way the board says it. Any real number clears the state, so no caller has to remember to.
- direction: Player asked to revert the blank counters - "Why did we even make that change in the first place?" - and said two plays a round feels thin, asking for more.
- decision: REVERTED. renderPreview() calls setTally(res.chips, res.mult) again; setTallyPending(), the .tally.pending rule and the #tpend caption are gone. The constraint it served is spec-the-verb-is-join's "the board never shows the score of an uncommitted line", which nobody asked for and which reads as a broken counter however it is dressed. The beads still say what each pair is DOING, which is the part that has to be learned; the counters say what it is worth.
- note: The Demand's own header still quoted "+25 points" after DEMAND_CHIPS went to 55 and gained a multiplier. It reads both constants now.
- decision: PLAYS_PER_ROUND is 3. Two was the rebuild's reading of "fewer and heavier than four" and it left the round no middle: best line, second best line, done, with no turn that hinges on what the first play spent - which is the turn the dull rule exists for. Eighteen plays a run against thirty-two, and against twelve.
- event: DEFECT, found by the change and older than it. G.dull[t] was set to FATIGUE_SPAN + 1 while the counter is decremented at the top of the NEXT play's bookkeeping, so an overtone was dull for three plays under a help panel that prints FATIGUE_SPAN and says two. Two plays a round hid it; three carries it over the Bookseller into the next round's opening hand. Now FATIGUE_SPAN, verified by counting the plays it is actually dull for.
- event: DEFECT. runTouched() tested G.plays < 4, true on the opening deal ever since the round stopped being four plays, so a board nobody had touched still asked whether you were sure you wanted to leave it. Reads PLAYS_PER_ROUND and DISCARDS_PER_ROUND now.
- decision: THE CLOCK stays in DISARMING. One play fewer out of three leaves two, which is a squeeze rather than a wall, so it looked like it should come out. Measured: letting it into the early Reckoning slots moved round 4 - SCHOLAR's first Ordeal round and already the run's wall - from 18.7% to 21.0%, past the 20% cap. The list governs what may be drafted ON TOP of a round that already has an Ordeal, not the rule on its own.
- decision: TARGETS re-derived by sweeping tools/balance.js --targets: 10,422 opening, x1.42 a round -> [10422, 14799, 21015, 29841, 42374, 60172]. The ratio had to rise as well as the base (1.30 -> 1.42) because the third play is worth most in the late rounds, where a stacked Lens gets a third firing, and least in round 1, where there is no engine. A uniform scale that fit round 1 left the last rounds free; one that fit the last rounds walled off round 1.
- decision: SCHOLAR curve[3] 0.97 -> 0.873. Round 4 is the mode's first Ordeal round and first Reckoning slot and has always been the wall; three plays made it a taller one, because an Ordeal that shrinks the hand or takes the discards costs more across three plays than across two. It ended 20.2% of runs, past the 20% cap. 17.6% now.
- note: The live region announces what the selection is worth again, now that the counters do. It had said the shape of the line and never the price since the JOIN rebuild took the price off the board.
- note: FINAL, n=500, --play optimal --buy value: APPRENTICE 51.2% (band 48-62, worst pre-final round 9.2% against a 15% cap), SCHOLAR 29.2% (band 25-38, worst pre-final round 17.6% against a 20% cap).
- direction: Player report - "the tutorial literally forces me to play a turn".
- decision: CAP-2 EXTENDED. The PLAY step had no Next, so the only way past it was Skip, which ends the lesson: a first-timer who did not want to commit a line they had not chosen had to quit the thing teaching them. Steps may now be SOFT - they still advance the instant the player does the thing, and they also carry a Next, labelled for what it is ("Not yet"). The PLAY step is the only one: the other two waits cost nothing and a second tap undoes them.
- note: The refund is untouched and still correct - a player who does not play is owed nothing, and refundTutorialPlay() already returns early on !tutSpentPlay. Verified in the browser: reaching step 8 via "Not yet" leaves G.plays at 3 and G.roundScore at 0, with the two taught words still selected and PLAY enabled.
- note: walkTutorial() in tests/e2e/ux.spec.js advanced on "a Next is visible", which after this change skipped the play it was there to make. It now reads TUT[tutStep].done and lets the wait win, so the refund specs still walk the path that accepts the invitation.
- event: E2E desktop 167 passed / 16 failed / 7 skipped, from 165/17/7. Node unchanged at 141/29. The remaining failures are the pre-JOIN backlog.
- direction: Player asked for the play to be taken out of the tutorial entirely rather than made optional, and for the tutorial and How to play to be rewritten in plain language with no jargon.
- decision: CAP-2 SIMPLIFIED, NOT EXTENDED. The lesson no longer asks for a PLAY at all. play() and discardSel() return early while tutActive(), and both buttons carry the reason in their title, so the keyboard (Enter, D) is barred by the same guard as the buttons.
- decision: DELETED with it: tutSpentPlay, tutRefundable, G.tutRefunded, refundTutorialPlay(), the play() hook that set the flags, the G.heldResult deferral and its landing in endTutorial(), and the two harness getters. Every one of them existed to keep the refund honest, and the refund existed to repay a play the lesson no longer takes. The Help-replay exploit is gone by construction rather than by gate.
- decision: Step 7 is 'The two buttons' now - it describes PLAY and DISCARD instead of spending its whole span demanding one of them, and says outright that both are off until the lesson ends.
- note: DEFECT FOUND. Step 5 reads the player's line back to them and is captioned 'tap the words in a different order and watch', but it was drawn once when the step opened and never again - so it described the two words that were down at that moment while the player laid a third. Steps carry a `live` flag now and toggleSel() redraws them.
- note: DEFECT FOUND. The tutorial's Demand step quoted a hardcoded +25 and had done since before DEMAND_CHIPS went to 55 and gained DEMAND_MULT. It reads both constants now.
- decision: COPY REWRITTEN in plain language, per /humanizer: short sentences, one idea a step, no em dashes, and the game's own vocabulary introduced rather than assumed. A first-timer is told a word has tags before being told the game calls them overtones, rather than being handed a guitar metaphor and the word 'rings of'. Same for the help panel, which is titled How to play now rather than How Overtone works.
- event: THREE STALE COPY ASSERTIONS FIXED as a side effect, all of them in the pre-JOIN backlog. The rules card was asserted to say 'Take three words' (a line has not been three words for two rebuilds), the why block to say 'never scores a point' (which the JOIN rebuild made false - meaning is the only thing that scores), and the tutorial's step 5 to name a figure (it names the pairs now). Help rule 4 also still claimed the board never tells you the total before you commit, which stopped being true when the counters came back.
- event: E2E desktop 169 passed / 13 failed / 7 skipped, from 167/16/7. Phone 174 passed / 14 failed / 1 skipped. Node unchanged at 141/29. The remaining failures are the pre-JOIN backlog.
- event: DEFECT FIXED. The board shrank on PLAY. #app.resolving .stage carried a min-height floor, and the JOIN readout made the stage at rest taller than the floor - so emptying the hint for the scoring dropped it from 311px to 267px on the one press where the board is supposed to take the room. play() pins the stage to the height it already had and clears the pin when the hand lands.
- event: DEFECT FIXED. An iPhone SE (375x667) put the hand's second row 14px inside the control bar and ran the document 18px past the viewport. Card height 86 instead of 98 below 700px tall clears both; nothing taller sees it.
- event: DEFECT FIXED. On a 393x727 portrait phone the row naming what the rest of the hand would do was 64px - the label had a line to itself and the rows were set at 1.85 - which pushed the hand 5px under the controls the moment a second card was picked, which is every turn. Inline label and tighter rows: 40px, same words.
- note: DEFECT FIXED. HAND_SHAPE.entropy still read "each shorter than the last", from when ENTROPY counted letters. It counts overtones.
- decision: The two Bookseller specs reached the shop by playing three blind hands, which the current curve no longer clears. They play one real hand and then clear the target, because they are about the shop and not about whether three arbitrary cards beat round 1. The shelf count excludes the kept line, which is a fourth .offer and not part of the shelf.
- note: The shelf-reality probe was re-run: probe2 at round 4 on SCHOLAR, where a bare deck reaches 24,954 against 26,051 and ALCHEMIST takes it to 27,954. Fifth time that fixture has moved; the seed and round are found by probing, never guessed.
- note: The landscape-hand spec bucketed card tops on a 40px grid to count rows. The cards are fanned across 13px, which straddled a boundary and called one row two. It measures the spread against the card height now.
- note: DEFECT FIXED, player-facing. How to play stated the keep rule wrong twice in one sentence: it said the Bookseller SELLS you a line and that kept lines come out of the five slots the Lenses use. A kept line is free and KEEP_SLOTS has been its own shelf since the decision above - the copy was true before that split and nobody re-read it. It now reads KEPT_CHIPS, KEPT_MULT, KEEP_SLOTS and LENS_SLOTS off the constants, and says the bonus binds to the HAND rather than to those words, which is the part the shop card does not make obvious either.
- note: Two comments went with it - the one on G.kept and the one over slotsUsed() - both still describing the shared shelf.
- decision: The rules card is pinned by e2e now. The wrong version was wrong in a way a reader would not catch, because it had been true; a test that reads the constants is the only thing that would have caught the drift.
- decision: The dull rule is on the board now. An overtone that has just scored renders faded and struck through on the card, with the number of plays until it wakes on hover. It was a rule the help panel stated, the tutorial stated, and the board never showed - a resting tag was drawn identically to one that pays.
- note: DEFECT FOUND doing it, and worse than the missing state. cardEl() computed the gold MATCH class from G.demand.tags alone, while demandHits() has always excluded dull tags - so a tag the round wanted, mid-rest, was drawn in gold and paid nothing. The board was promising points it had already decided not to pay. Resting now beats matching, and the sort that puts scoring tags first (so the phone's two-line cap never hides one) reads the same test.
- note: Opposition is never dulled, which is why the tag is faded rather than removed: a resting overtone can still clash with the word beside it.
- direction: Player on the resting treatment: "I dont understand the resting thing, it doesnt look different or anything". Correct - the first try only lightened the lilac chip and added a hairline strike, and a live tag is already pale lilac on a cream card, so going PALER moved it toward the card instead of away from it. Side by side at card size the two were the same thing.
- decision: A resting overtone is a GREY chip with light text and a 1.5px strike, not a paler lilac one. Three states that cannot be confused at a glance: gold pays the round, lilac pays a join, grey struck pays nothing. Compared five treatments on a real card at card size before picking.
- direction: Player on the rules card: "properly seperate and write the how to play. the 5th line has both dull functionality and then the bookseller offering that line thing" - and, separately, "add an option to see the line we chose again".
- decision: HELP RULE 5 SPLIT INTO 5 AND 6. It carried two rules under one number: a tag going quiet on the table, and the Bookseller handing a line back in the shop. Different places, different moments, nothing in common but the paragraph. Rule 5 is resting alone and now describes the treatment a player can actually see (grey, struck through) rather than the word "dull", and says outright that opposites never rest, which is the part that changes how you play. Rule 6 is the keep. The subhead reads Six rules.
- decision: LINES YOU KEPT ARE ON THE BOARD. A kept line was written down in the shop and then never shown again until the next shop - a rule the player is playing under, invisible for a round at a time, and the only rule in the run they wrote themselves. A strip under the Lens rail, inside the same slab, gold because every payout a kept line makes is printed in gold. Closed it is the words; tapped it says the hand it binds to and what it pays, same open-one-at-a-time idiom as a Lens so there is no second interaction to learn.
- decision: SEPARATE SHELF, not the Lens rail. Keeps and Lenses have separate counts and separate slots, and one row of chips holding both would say they compete for the five. The strip is hidden outright when nothing has been kept, so it costs the stage nothing on a board that has none.
- note: The kept chip flashes when it fires, the way a Lens does. The event carries its index now. Without it the biggest engine in the run paid out of nowhere.
- note: DEFECT FOUND AND FIXED IN THE SAME CHANGE. .keptbar sets display:flex on a class, which beats [hidden] in the cascade, so the strip drew its rule and its KEPT label on every board in the game whether or not anything had been kept. Caught by the e2e assertion that the strip is hidden before the first keep, not by looking. Every other hideable thing on the sheet already carried the [hidden] guard.
- note: Measured on a 393x727 phone with five Lenses and two kept lines, the worst case there is: the strip costs the stage 28px and the hand nothing, because the stage flexes. Hand-to-controls clearance is 4px with the strip and 4px without it.
- event: Node 165/165. E2E 376 passed / 0 failed / 8 skipped at --retries=0, from 372/0/8 - the two new specs across both projects.
- direction: Player: "Yea do that" on the resting-clock defect flagged above, plus "commit everything on github properly" and "rewrite the readme in simple non ai jargon".
- decision: DEFECT FIXED. Only an overtone that actually PAID is put back to sleep. Both paths into the fatigue map ignored whether the tag was resting already: the pair loop was handed {} so it would ignore rest on purpose, and the demand loop never asked. joinOf drops a resting tag from the shared tags and demandHits refuses it, so a resting tag pays nothing - and the code then reset its clock to FATIGUE_SPAN anyway. Playing the same overtone repeatedly held it asleep forever, which made the card's own "wakes up in 2 plays" a promise the board could break on the next hand. Same shape as the gilding bug: charging for something it had already declined to pay for.
- note: The fix takes a snapshot of G.dull BEFORE the counters are decremented, because that is the map resolve() priced the line against. The pair loop is then handed wasResting instead of {}, which is exactly the set of overtones that actually paid resonance.
- note: It is a mild buff - overtones now recover on the schedule the card states. e2e covers both paths and was checked against the broken version: it fails there and passes here.
- decision: README REWRITTEN. It still described the pre-JOIN game: three words, +25 a match, THE COLUMN / THE STAIR / THE CHAIN, eight rounds, four plays, 24 Lenses, Ordeals on 4, 6 and 8. All of it wrong since the JOIN rebuild, and nobody had re-read it. 589 lines to 402, plain language throughout per /humanizer (no em dashes, no bold-header lists, no rule-of-three padding), every number re-read off the constants or the harness rather than carried over.
- note: Three counts in the old text were wrong even for their own era and are corrected: seven flawed Lenses not six, nine growing not seven, and the win rates are the current measured pair (APPRENTICE 51.2%, SCHOLAR 29.2%, n=500) rather than the pre-JOIN ones.
- note: TWO FEATURES THE README NEVER MENTIONED are in it now: why the verb became JOIN (the diagnosis and the three lexicon numbers that made it possible), and "play it blind", which has shipped as a switch for a while with no documentation anywhere outside the settings panel.
- note: .claude/ is gitignored rather than committed. It is local tool config and does not belong in the repo.
- event: Node 165/165. E2E 378 passed / 0 failed / 8 skipped at --retries=0.
